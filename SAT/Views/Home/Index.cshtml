@model IEnumerable<SAT.Models.tbDepartamento>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="x_panel">
	<div class="x_title">
		<h2>Index</h2>
		<div class="nav navbar-right panel_toolbox">
			@Html.ActionLink("Resgitra departamento", "Create", null, new {@class ="btn btn-info"})
		</div>
		<div class="clearfix"></div>
	</div>
<table class="table table-striped table-bordered dataTable" id="TableGrid">
<thead>
	<tr>
        <th>
            @Html.DisplayNameFor(model => model.dep_Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.dep_UsuarioCrea)
        </th>
        <th></th>
    </tr>
</thead>
<tbody>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.dep_Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.dep_UsuarioCrea)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.dep_Codigo }, new {@class = "btn btn-default btn-xs"})
            @Html.ActionLink("Detalle", "Details", new { id=item.dep_Codigo }, new {@class = "btn btn-default btn-xs"})
        </td>
    </tr>
}
</tbody>
</table>
</div>
